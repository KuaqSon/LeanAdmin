@model IEnumerable<LeanAdmin.Models.Question>
@{
    ViewBag.Title = "Index";
}

<h2>Questions</h2>

<div class="row">
    <div class="col-sm">
        <button class="btn btn-success">Add <i class="fas fa-plus-circle"></i></button>
        <button class="btn btn-danger" type="button" data-url="@Url.Action("Delete")">Delete </button>
    </div>
    <div class="col-sm-3">
        <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
    </div>
</div>

<br />
<div>
    <table class="table">
        <tr>
            <th>Name</th>
            <th>Decription</th>
            <th>Control Type</th>
            <th>Update Date</th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Label)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ControlId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.UpdatedDate)
                </td>
            </tr>
        }
    </table>
</div>